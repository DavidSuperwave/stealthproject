# ============================================================
# Jaime AI — Environment Variables
# Copy to .env.local and fill in your values
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── LipDub API (server-side only — proxied via /api/lipdub) ──
LIPDUB_API_KEY=your-lipdub-api-key
# Optional: override the LipDub base URL (defaults to https://api.lipdub.ai/v1)
# LIPDUB_API_URL=https://api.lipdub.ai/v1

# ── App URL ──────────────────────────────────────────────────
# REQUIRED in production — used for Stripe redirect URLs and email links
# Defaults to http://localhost:3000 if not set
NEXT_PUBLIC_APP_URL=https://doblelabs.com

# ── Stripe (server-side + client) ────────────────────────────
# Use sk_test_ / pk_test_ for development, sk_live_ / pk_live_ for production
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# Webhook signing secret — create webhook at:
#   https://dashboard.stripe.com/webhooks → Add endpoint
#   URL: https://doblelabs.com/api/stripe/webhook
#   Events: checkout.session.completed
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# ── Admin access ─────────────────────────────────────────────
# Single admin user — use your Supabase auth user UUID
# Set the same value for both (NEXT_PUBLIC_ is used for UI visibility only)
ADMIN_USER_ID=your-supabase-user-uuid
NEXT_PUBLIC_ADMIN_USER_ID=your-supabase-user-uuid

# ── Cron secret (Vercel Cron job authentication) ─────────────
# Required if using vercel.json cron for poll-generation
CRON_SECRET=your-cron-secret

# ── Email notifications via Resend (optional) ────────────────
# Logs to console if not set
# RESEND_API_KEY=re_your-resend-key
# EMAIL_FROM=Jaime AI <noreply@doblelabs.com>
